{
  "name": "stetson",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "module": "index.js",
  "scripts": {
    "test": "node index.test.js",
    "size": "npx esbuild --bundle --minify --external:svelte --format=esm index.js | gzip -c | wc -c"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.12",
    "svelte": "^4.2.9"
  }
}
